export enum LoaderType {
  big = "fixed left-[calc(50%-40px)] top-[calc(50%-40px)] h-20 w-20",
  small = "h-8 w-8",
}

export enum FieldFormValue {
  nickname = "nickname",
  email = "email",
  password = "password",
}
